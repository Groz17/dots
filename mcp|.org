* MCP servers
#+name: MCP_YAML
#+begin_src yaml :noweb yes
gh_grep:
  type: remote
  url: https://mcp.grep.app
context7:
  type: local
  command: [ "npx", "-y", "@upstash/context7-mcp" ]
fetch:
  type: local
  command: [ "uvx", " mcp-server-fetch" ]
emacs:
  type: local
  command: [ "{env:HOME}/.config/emacs/elpaca/repos/emacs-mcp-server/mcp-wrapper.sh", "{env:HOME}/.config/emacs/emacs-mcp-server.sock" ]
# vim:
#   type: local
#   command: [ "npx", "buddy-mcp-proxy" ]
# pdf-reader-mcp:
#   type: local
#   command: [ npx, '@sylphlab/pdf-reader-mcp']
#+end_src
# npx -y github:nietus/anki-mcp

#+name: MCP
#+begin_src bash :noweb yes :results raw
<<'EOF' yq -oj|head -c -1
<<MCP_YAML>>
EOF
#+end_src
